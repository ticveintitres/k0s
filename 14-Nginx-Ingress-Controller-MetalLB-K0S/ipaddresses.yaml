# Script realizado con Amor por:
# Alejandro Martinez Capilla
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: ingress-nginx
  namespace: metallb-system
spec:
  addresses:
  - 10.23.23.100/32 # Aqui la IP que quiero que tenga siempre NGINX
  autoAssign: true
  serviceAllocation:
    namespaces:
    - ingress-nginx